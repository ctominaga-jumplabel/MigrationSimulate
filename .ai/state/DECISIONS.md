# DECISIONS (append-only)

Formato: `[data] decisão — motivo — alternativas descartadas`

- [2026-06-15] Stack = Streamlit (Python) — interatividade (sliders, edição de prioridade, sprints) com baixo custo de construção e Python já disponível — descartado: Excel avançado (sem interatividade rica), HTML/JS estático (mais código p/ manter).
- [2026-06-15] Duplicatas exibidas como **dois cenários lado a lado** (Bruto vs Sem-dup) — decidir escopo é do cliente; evita esconder os 71% de duplicatas — descartado: usar só números brutos, ou só colapsar.
- [2026-06-15] Granularidade de prioridade/sprint = **por EGP (pipeline) + órfãos avulsos** — aderente à realidade da migração (EGP vira um Job no Databricks) — descartado: por .sas individual (espalharia um pipeline por vários sprints).
- [2026-06-15] Alavancas na app = nº consultores, J_base, J_task, horas/dia, K — cobrem os drivers principais — descartado (por ora): penalidade de coordenação de time (efeito Brooks).
- [2026-06-15] Defaults: prioridade padrão = maior esforço primeiro; horas/dia = 6; J_base = 8h; J_task = 2h; K = 1.0 — pontos de partida razoáveis, todos editáveis na UI.
- [2026-06-15] Esforço por EGP = Σ horas_sas + (J_base + J_task × n_sas) — separa migração de código (já estimada) do overhead de orquestração no Databricks.
- [2026-06-15] Cenário "Sem duplicatas" = colapsa famílias de versões para o EGP canônico (maior esforço) usando regras de sufixo do §6 da metodologia, e conta só .sas com is_likely_duplicate=False — alinhado à metodologia existente.
- [2026-06-15] Arquitetura em 3 camadas (ETL → core puro → UI) — cálculo testável fora do Streamlit — descartado: lógica embutida na UI.
- [2026-06-15] Calendário de sprints pula só fins de semana na v1 (sem feriados) — simplicidade; feriados ficam no backlog.
- [2026-06-15] Fase 0 concluída: ETL via `prepare_data.py` → `data/dataset.parquet` + `data/egp_rollup.parquet`. Rollup guarda `n_sas`/somas (Bruto e sem-dup a nível de arquivo) SEM overhead de Job (calculado em core.py). Validado por reviewer+QA (fidelidade exata vs planilha).
- [2026-06-15] `pipeline_family` (colapso de versões) ampliada após review: + padrões ` - Copy`/`(Recovered Copy)`/` v2`/` - v2`/`_bkp\d`. Resultado: 3.198 EGPs → 1.516 famílias (antes 1.516 vs 1.587). Heurística assumidamente aproximada — validar amostra com cliente (risco em IDEACAO §7) — descartado: dedup por hash de conteúdo a nível de EGP (mais custoso; pode entrar no backlog).
- [2026-06-15] Cenário Sem-dup a nível de EGP/família será resolvido em core.py (Fase 4): EGP canônico = maior esforço da família retém horas; versões colapsadas zeram. 1.062 EGPs já têm n_sas_sem_dup=0 no rollup.
- [2026-06-15] Fase 4 (core.py): motor `compute_scenarios` + `pack_sprints`; funções puras migradas de app.py para core.py; `load_data` órfã removida (cache único). app.py só orquestra UI. Reviewer+QA aprovaram após correções.
- [2026-06-15] Definição ÚNICA de "Sem duplicatas" = por-família (canônico = maior `soma_horas_sas`, via `core.canonical_rollup`), consumida por compute_scenarios E egp_table. Elimina os 3 números divergentes (família 63.779 .sas vs file-level 115.002 vs sem-colapso 80.747). `overview_metrics.sem_dup` renomeado p/ `sem_dup_arquivo` (só caption descritivo) — descartado: manter file-level como KPI lado a lado.
- [2026-06-15] Bug corrigido: `duracao_dias_uteis` dividia por horas_dia 2x; agora = esforco_total/(n_consultores×horas_dia). `duracao_horas` redefinido = duracao_dias_uteis×horas_dia (esforço por consultor).
- [2026-06-15] K aplicado ao esforço total e ao `horas` de cada item da fila (Σ sprints == esforço_total×K). Esforço Bruto (defaults 5/6/8/2/1.0) = 311.532h (1.039 sprints); Sem-dup = 133.661h (446 sprints).
- [2026-06-15] Capacidade=0 (n_consultores=0): pack_sprints retorna 0 sprints, concordando com n_sprints=0. UI tem mínimo 1 consultor.
- [2026-06-15] Fase 5 (aba Sprints): prioridade editada pelo usuário (session_state por cenário) realimenta a fila via `compute_scenarios(..., prioridades=)`; itens ausentes do override caem ao fim por esforço. Reviewer+QA APROVADO (20 testes).
- [2026-06-15] Fase 6 (final): seletor de cenário UNIFICADO na sidebar (global), em vez de um por aba — mais previsível; persistência de prioridade segue por cenário. Export do plano em xlsx (4 abas: egps/orfaos/resumo_sprints/alocacao) e CSV via `core.build_export` puro. README + requirements.txt criados. 24 testes. Gate final APROVADO — v1 pronta.
- [2026-06-15] PROJETO v1 CONCLUÍDO. Todas as 6 fases passaram por data-engineer/streamlit-dev → reviewer → qa → snapshot. Fidelidade numérica reconciliada com a planilha (diff 0).

## v2 — Transformação do frontend (Cogna Migration Mission Control)
- [2026-06-15] Frontend premium = **Next.js (App Router) + TS + Tailwind + Framer Motion + Iconsax + Zustand + React Query + Recharts**, em `frontend/`. Streamlit (`app.py`) preservado como legado — não quebrado. Plano em `.ai/state/FRONTEND_TRANSFORMATION_PLAN.md`. Agentes criados: frontend-architect, product-designer, motion-designer, frontend-dev, visual-qa.
- [2026-06-15] Integração de dados = **API Python fina (FastAPI) em `api/`** que embrulha `core.py` — decisão do usuário (alternativa descartada: export JSON estático, que perderia o recálculo ao vivo das alavancas; e híbrido, mais complexo). Regra-mestre: **nenhum cálculo de negócio é reimplementado no frontend** (AIOS §10). A API só serializa o que `core.*` calcula; sanitiza NaN/inf→null e date→ISO. Serialização `.xlsx` vive na API (espelha `app._export_to_xlsx`), mantendo `core` puro.
- [2026-06-15] Contrato da API: `GET /api/health|/api/catalog`; `POST /api/scenarios|/api/egps|/api/egps/children|/api/orphans|/api/sprints|/api/export`. `params` (alavancas + cenario + prioridades) no corpo; prioridades chegam como lista `{tipo,nome,prioridade}` e são aplicadas SÓ ao cenário ativo (espelha persistência por-cenário do Streamlit). CORS restrito a localhost:3000.
- [2026-06-15] Navegação de 8 áreas: Overview · Scenario Builder · Pipelines (EGPs) · SAS Órfãos · Sprint Planner · Timeline · Analytics · Export. Cenário ativo é GLOBAL (Topbar), espelhando o seletor único da sidebar do Streamlit.
- [2026-06-15] Fidelidade reconfirmada via API (defaults): Bruto 311.532 h / 1.039 sprints; Sem-dup 133.661 h / 446 sprints — batem com a v1. Gates v2 APROVADOS: pytest 24 passed; typecheck limpo; `npm run build` 11/11 páginas; rotas servem HTTP 200.
- [2026-06-15] Deploy (demo pontual) = **Vercel (frontend) + Render (API)** — escolha do usuário (nuvem pública gerenciada). Artefatos: `render.yaml`, `.gitignore` raiz (exclui xlsx/.sas/.egp crus, mantém data/*.parquet), `frontend/middleware.ts` (Basic Auth opt-in via env), `DEPLOY.md`. CORS da API agora por env (`FRONTEND_ORIGIN`/`FRONTEND_ORIGIN_REGEX`); `api/requirements.txt` autossuficiente (sem streamlit/plotly). Insight: as 8 telas são client-only → frontend poderia ser estático; só a API precisa de servidor. Risco aceito: endpoints da API ficam abertos por URL (mitigável com token; suficiente p/ demo). Alternativa documentada: túnel local (Cloudflare/ngrok).
- [2026-06-16] Deploy EXECUTADO e NO AR: https://migration-simulate.vercel.app (Vercel CLI, prod, a partir de `frontend/`). Validado em produção (paridade idêntica). Repo GitHub tornado privado antes do push (era público — risco de sigilo flagrado). Next 15.1.6→15.5.19 (Vercel bloqueia CVE). Auto-deploy por git PENDENTE: conectar repo privado exige autorizar o GitHub App da Vercel no navegador (OAuth, não-headless) + setar Root Directory=`frontend` no dashboard. `vercel git connect` via CLI falha sem essa autorização.
- [2026-06-15] Deploy REVISADO → **TUDO no Vercel** (escolha do usuário: uma plataforma só). API FastAPI vira função serverless `frontend/api/index.py` chamada em same-origin (`/api/*` via rewrite no `frontend/vercel.json`). Para caber no limite de 250MB e dispensar `pyarrow`: motor vendorizado por script (`frontend/scripts/build_engine.py` copia core.py/models.py/serializers.py VERBATIM p/ `frontend/_engine/` e converte parquet→CSV enxuto ~6MB). `core.py` da raiz segue única fonte editável e testada (pytest). Cliente: `NEXT_PUBLIC_API_BASE` default "" (same-origin); `.env.local` mantém localhost p/ dev. Wrapper ASGI `EnsureApiPrefix` torna o roteamento robusto às 2 formas de path do Vercel. Validado LOCALMENTE: paridade idêntica via CSV (Bruto 311.532/1039, Sem-dup 133.661/446), wrapper roteia `/api/*` e `/*`, `npm run build` ok (middleware 32kB). NÃO testável daqui: empacotamento/roteamento real do Vercel (documentado em DEPLOY.md). Render/split vira alternativa de referência.
